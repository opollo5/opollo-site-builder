## Home

The home hero should include a code snippet:

```js
const greeting = "hello";
console.log(greeting);

## About

This is the about section that never gets reached if we treat the fence strictly. But we parse pragmatically and keep the page breaks.
