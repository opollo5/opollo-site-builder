node_modules
.next
out
build
dist

.env
.env.local
.env.*.local

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
next-env.d.ts
*.tsbuildinfo

# M1c extraction sources are one-time artefacts — don't commit them. The
# extracted components/templates under seed/leadsource/ ARE committed; only
# the raw live-site HTML we extract from is excluded.
seed/leadsource/source/
