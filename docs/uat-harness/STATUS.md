# UAT Harness — PR Status Log

---

## PR 1: feat(uat): sign-in bypass route + Playwright auth helper

- URL: (pending merge)
- Merge SHA: (pending)
- Deploy: (pending)
- Specs added: 0 (infrastructure only)
- Known failures opened: 0

### What shipped

- `app/api/uat/sign-in/route.ts` — POST endpoint, env-guarded (404 in production), bearer-token auth via `STAGING_UAT_SECRET`, password-auth primary + magic-link fallback
- `e2e/uat/helpers/auth.ts` — `signInAsUatBot(page)` + `navigateAsUatBot(page, path)` helpers
- `lib/rate-limit.ts` — `uat_sign_in` limiter (100 req / 5 min / IP, cosmetic)
- `middleware.ts` — `/api/uat/` prefix added to `isPublicPath` (pre-session reachable)
- `.github/workflows/button-migration-gates.yml` — Gate 8: UAT route env-guard + secret check

### Required action from Steven before PR 2

| Item | Where |
|---|---|
| Set `STAGING_UAT_SECRET` in Vercel (staging branch) | Vercel → Project → Settings → Environment Variables → Git Branch = `staging` |
| Set `STAGING_UAT_SECRET` as GitHub Actions secret | GitHub → Settings → Secrets and variables → Actions → New secret |

The route returns HTTP 500 until `STAGING_UAT_SECRET` is set in Vercel. The Playwright helper throws until `STAGING_UAT_SECRET` is set in the CI environment.

---

## PR 2: feat(uat): critical path UAT spec library

- URL: (pending merge / rebase after PR 1 merges to staging)
- Merge SHA: (pending)
- Deploy: N/A — spec-only PR
- Specs added: 8 files, ~60 tests (P0/P1/P2)
- Known failures opened: KF-0A through KF-6

### What shipped

- `playwright.uat.config.ts` — UAT Playwright config (staging base URL, Vercel bypass header, trace/video/screenshot on, visual snapshot path)
- `e2e/uat/helpers/report.ts` — `saveFailureReport()` + `collectConsole()` helpers
- `e2e/uat/auth.spec.ts` — P0: sign-in, protected route, sign-out
- `e2e/uat/calendar.spec.ts` — P0: grid render, today cell, month nav, chip click → composer
- `e2e/uat/composer.spec.ts` — P0: new-post, edit-mode (regression), AI dialog (regression), media library (regression), panels, date picker width (regression), save/discard-on-close (regressions), draft
- `e2e/uat/connections.spec.ts` — P0: list, status pills, disconnect dialog, reconnect OAuth
- `e2e/uat/posts.spec.ts` — P1: list, draft/scheduled/published filters, click-to-composer
- `e2e/uat/media-library.spec.ts` — P1: admin/images, search, filter, count regression
- `e2e/uat/admin.spec.ts` — P1: sites/users/companies/health/theming
- `e2e/uat/insights.spec.ts` — P2: analytics, insights, period selectors
- `docs/uat-harness/known-failures.md` — KF-0A through KF-6
- `playwright.config.ts` — `**/uat/**` added to `testIgnore` (main e2e suite targets localhost; UAT specs target live staging and must not run there)

### Required action from Steven before running full suite

| Item | Where |
|---|---|
| Set `VERCEL_BYPASS_SECRET` in GitHub Actions secrets | GitHub → Settings → Secrets → `VERCEL_BYPASS_SECRET` |
| Set `VERCEL_BYPASS_SECRET` in Vercel (staging) | Vercel → Project → Settings → Deployment Protection → Protection Bypass for Automation |

Without `VERCEL_BYPASS_SECRET`, all page navigation returns HTTP 401 from Vercel SSO (KF-0B).

---

## PR 3: feat(uat): CI harness + visual regression

- URL: (pending push)
- Merge SHA: (pending)
- Deploy: N/A — CI/spec-only PR
- Specs added: 1 file (`visual.spec.ts`), 6 visual regression tests (P1)

### What shipped

- `.github/workflows/uat-harness.yml` — two jobs:
  - `uat-p0`: P0 gate on every PR (non-blocking outside staging target; auto-posts result to PR comment)
  - `uat-full`: push/schedule/dispatch full suite (P0+P1+P2); seeds UAT data, regenerates baselines on `regen-baselines` commit message, auto-opens GitHub issue on failure, Slack webhook on nightly failure, archives screenshots on merge-to-main/staging
- `e2e/uat/visual.spec.ts` — P1 visual regression: calendar grid, composer empty, composer populated, AI dialog, date picker, admin theming, admin image library grid
- `docs/uat-harness/latest-report.html` — static report placeholder; regenerated by `uat-full` job on each run
- `docs/uat-harness/STATUS.md` — this file, PR 2 + PR 3 entries added

### Post-merge: rebase sequence

1. PR 1 merges to `staging`
2. PR 2: `git rebase staging feat/uat-critical-paths` → force-push → `gh pr edit 1031 --base staging` → CI re-runs
3. PR 3: `git rebase feat/uat-critical-paths feat/uat-harness-ci` → force-push → CI re-runs

---
